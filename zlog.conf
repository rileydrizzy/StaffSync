[formats]
simple = "%d %V [%p:%f:%L] %m%n"

[rules]
# Log everything of "info" level or higher to stdout
my_cat.INFO    >stdout; simple
# Log everything of "debug" level or higher to a file
my_cat.DEBUG   "logs/app.log", 1MB * 5 ~ "logs/app.log.%d(%Y%m%d).#2s"; simple